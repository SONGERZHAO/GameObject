using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pick_food : MonoBehaviour {

	
	// Update is called once per frame
	void Update ()
    {
        Vector3 tmp = new Vector3(1, 1, 1);
        transform.Rotate(tmp);
	}
}
